# 更新日志

## MIUIX-test（基于 v1.2.1 / main 的 UI 全面重写分支）

> 对比 main（v1.2.1）：85 个提交，74 个文件变更（+12478 / −1140）。

### 全新双模式 UI

- 引入 **Miuix（HyperOS 风格）与 Material 3 Expressive 双 UI 引擎**，设置内即时切换界面风格，两套独立主题、组件与页面外壳
- 首页、配置列表、设置、主题取色四个页面均为双实现，观感随所选风格整体切换

### 主题系统（对齐 KernelSU Manager）

- 主题设置页重构为三组结构：**主题模式**（跟随系统 / 浅色 / 深色 / AMOLED 纯黑）、**强调色**（16 种预设色块 + 跟随系统动态取色）、**色彩风格与标准**（13 种调色板 + Spec2021/2025）
- 基于 materialkolor 的动态取色（Material You HCT 方案），切换强调色时全局主题色弹簧过渡动画
- 进程级取色方案预热缓存：色块行随页面即时呈现，切换时主线程零取色计算
- 设置仓库改为快照可观察：所有设置项的界面状态与实际生效值实时同步

### 悬浮底栏与液态玻璃（自 KernelSU 移植）

- 悬浮胶囊底栏：阻尼拖拽动效、弹簧导航、压感高光与回弹
- 液态玻璃视觉层：lens 折射、vibrancy 活力模糊、内阴影
- 宽屏自适应 Navigation Rail（跟随 WindowSizeClass）

### 原生 JSON 编辑器

- 编辑器核心重写为原生 EditText（NativeJsonEditorView）：行号栏、捏合缩放、语法高亮，长文档输入零延迟
- 行号栏改用 Layout 的逐行真实基线定位（原按标称行高估算，行号越往下与正文偏得越多）
- 捏合缩放统一走硬件层预览，手势期间不再每次采样触发全文重排（双指缩小掉帧的根因），最终字号在手势结束一次性落地
- 键盘避让：容器级 `imePadding` 复现窗口缩放几何，末行光标不再被软键盘遮挡
- 进场优化：转场期间延迟组合重型内容 + 装载期后台预高亮，页面切换全程顺滑
- 语法实时校验（250ms 防抖）：边打字边报、修好即消失，保存按钮随校验结果禁用；写入前再以严格 JSON 复核
- 未保存修改有确认弹窗与返回键拦截，编辑器外壳按 Miuix / Material 双皮肤实现

### 平板与横屏适配

- 宽屏「列表 + 编辑器」双窗布局；设置页横屏双窗（左侧设置项 + 右侧主题预览窗格）

### 数据库层（Rust）

- `libcosa.so` 改为 bundled SQLite 静态编译，**不再依赖目标设备系统 `libsqlite.so`**，修复 Android 13 等设备因系统库过旧导致的 CLI 无法加载问题
- 增加按库失败报告、触发器恢复与产物自检（`rust/build-android.sh`）

### HyperOS 云控适配（Joyose）

- 平台检测与动态分发：识别 HyperOS 后首页、底栏与路由改走澎湃云控界面，ColorOS 路径不变
- **应用配置**列表（有 per-app 命中的应用、图标、搜索、宽屏双窗格）→ **应用功能页**（22 类功能卡片：中文标签、来源徽标、主开关 gate 提示、参数折叠、冲突警告）→ **通用配置**（全局布尔开关分组，写后 force-stop + 冻结防云控覆盖）
- 新增 `joyose-stat/list/read/write/freeze/unfreeze/backup/revert/apps/app/scoped/scoped-write` 命令族，绕开 MCC 云端直读直写 SmartP.db 与 teg_config.db
- **高级 JSON 编辑**：只编辑当前应用名下的云控片段。`joyose-scoped` 一次调用完成指针解析、片段抽取与格式化（App 进程不再搬运 346KB 整份文档）；保存走 `joyose-scoped-write`，按指针把片段就地补丁进当前文档再双库镜像写，未触碰的应用与全局开关逐字节不变；新增/改名/删除键由 CLI 直接报错，不存在静默丢弃

### 其他

- 支持预测性返回手势（重启应用后生效）
- 新增界面缩放调节
- 首页设备信息与系统属性展示对齐 KernelSU

### 工程与构建

- 工具链升级：Kotlin 2.4.10 / AGP 9.3.2 / Gradle 9.7.1 / Compose BOM 2026.08.00 / compileSdk 37
- debug 与 release 构建均启用 R8 混淆与资源收缩
- 导航层迁移至 Navigation 3（Navigator / Routes）
